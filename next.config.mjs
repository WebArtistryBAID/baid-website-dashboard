/** @type {import('next').NextConfig} */
const nextConfig = {
    i18n: {
        locales: [ 'en', 'zh-CN' ],
        defaultLocale: 'en',
        localeDetection: false
    }
}

export default nextConfig
